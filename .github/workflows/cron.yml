name: Run tests
on:  
  push:
  pull_request:
  schedule:
    - cron: '* * * * *'
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Print hello world
      run: echo "Hello World!!!"
